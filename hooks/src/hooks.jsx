import React from 'react'

const Hooks = (props) => {
  return (
	<div>
		<h1>{props.name}</h1>
		<h2>{props.job}</h2>
		<h3>{props.price}</h3>
		 hooks
		 
		 <div>
		<h1>{props.name}</h1>
		<h2>{props.job}</h2>
		<h3>{props.price}</h3>
		 hooks</div>
		 <div>
		<h1>{props.name}</h1>
		<h2>{props.job}</h2>
		<h3>{props.price}</h3>
		 hooks</div>
		 
		 </div>
		 
  )
}

export default Hooks;